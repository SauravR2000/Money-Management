enum ErrorCheckType {
  email,
  password,
  name,
}

enum TransactionType {
  expense,
  income,
}

enum Destination {
  confirmPincode,
  dashboard,
}

enum ImageCaptureType {
  camera,
  gallery,
}
